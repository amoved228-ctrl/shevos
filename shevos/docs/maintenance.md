# Maintenance

- Не смешивай много AUR-пакетов в ISO.
- Для AUR используй локальный репозиторий и `repo-add`.
- Перед релизом тестируй UEFI и BIOS.
- Проверяй ISO в QEMU и VirtualBox.
- Храни свои конфиги в `configs/`, а в `/etc/skel` копируй только проверенную версию.
